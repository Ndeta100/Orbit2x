{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "find . -name '*_templ.go' -delete && templ generate && go mod tidy && npm run build && go build -o orbit2x ."
  },
  "deploy": {
    "startCommand": "./orbit2x",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}